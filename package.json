{
  "name": "contact-form",
  "version": "2.0.0",
  "description": "Статическая форма заявки с отправкой на email через Web3Forms",
  "scripts": {
    "build": "node build.js",
    "preview": "npm run build && npx --yes serve dist -p 3000"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
